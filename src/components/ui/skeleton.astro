---
import { cn } from '@/lib/utils';

interface Props extends astroHTML.JSX.HTMLAttributes {}

const { class: className, ...props } = Astro.props;
---

<div class={cn('animate-pulse rounded-md bg-muted', className)} {...props} />
